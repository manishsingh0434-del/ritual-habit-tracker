import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.manish.ritual',
  appName: 'Ritual',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
